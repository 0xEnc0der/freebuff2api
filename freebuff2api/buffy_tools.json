[
 {
  "type": "function",
  "function": {
   "name": "spawn_agents",
   "description": "Use this tool to spawn agents to help you complete the user request. Each agent has specific requirements for prompt and params based on their tools schema.\n\nThe prompt field is a simple string, while params is a JSON object that gets validated against the agent's schema.\n\nEach agent available is already defined as another tool, or, dynamically defined later in the conversation.\n\n**IMPORTANT**: `agent_type` must be an actual agent name (e.g., `basher`, `code-searcher`, `opus-agent`), NOT a tool name like `read_files`, `str_replace`, `code_search`, etc. If you need to call a tool, use it directly as a tool call instead of wrapping it in spawn_agents.\n\nYou can call agents either as direct tool calls (using the listed tool name, e.g. `example_agent`) or use `spawn_agents` with the canonical agent name in `agent_type` (e.g. `example-agent`). Both formats work, but **prefer using spawn_agents** because it allows you to spawn multiple agents in parallel for better performance. Both use the same schema with nested `prompt` and `params` fields.\n\n**IMPORTANT**: Many agents have REQUIRED fields in their params schema. Check the agent's schema before spawning - if params has required fields, you MUST include them in the params object. For example, code-searcher requires `searchQueries`, basher requires `command`.\n\nExample:\n<spawn_agents_params_example>\n{\n  \"agents\": [\n    {\n      \"agent_type\": \"basher\",\n      \"prompt\": \"Check if tests pass\",\n      \"params\": {\n        \"command\": \"npm test\"\n      }\n    },\n    {\n      \"agent_type\": \"code-searcher\",\n      \"params\": {\n        \"searchQueries\": [\n          {\n            \"pattern\": \"authenticate\",\n            \"flags\": \"-g *.ts\"\n          }\n        ]\n      }\n    }\n  ]\n}\n</spawn_agents_params_example>",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Spawn multiple agents and send a prompt and/or parameters to each of them. These agents will run in parallel. Note that that means they will run independently. If you need to run agents sequentially, use spawn_agents with one agent at a time instead.",
    "type": "object",
    "properties": {
     "agents": {
      "type": "array",
      "items": {
       "type": "object",
       "properties": {
        "agent_type": {
         "description": "Agent to spawn",
         "type": "string"
        },
        "prompt": {
         "description": "Prompt to send to the agent",
         "type": "string"
        },
        "params": {
         "description": "Parameters object for the agent",
         "type": "object",
         "properties": {
          "command": {
           "description": "Terminal command to run (basher, tmux-cli)",
           "type": "string"
          },
          "what_to_summarize": {
           "description": "What information from the command output is desired (basher)",
           "type": "string"
          },
          "timeout_seconds": {
           "description": "Timeout for command. Set to -1 for no timeout. Default 30 (basher)",
           "type": "number"
          },
          "searchQueries": {
           "description": "Array of code search queries (code-searcher)",
           "type": "array",
           "items": {
            "type": "object",
            "properties": {
             "pattern": {
              "description": "The pattern to search for",
              "type": "string"
             },
             "flags": {
              "description": "Optional ripgrep flags (e.g., \"-i\", \"-g *.ts\")",
              "type": "string"
             },
             "cwd": {
              "description": "Optional working directory relative to project root",
              "type": "string"
             },
             "maxResults": {
              "description": "Max results per file. Default 15",
              "type": "number"
             }
            },
            "required": [
             "pattern"
            ],
            "additionalProperties": false
           }
          },
          "filePaths": {
           "description": "Relevant file paths to read (opus-agent, gpt-5-agent)",
           "type": "array",
           "items": {
            "type": "string"
           }
          },
          "directories": {
           "description": "Directories to search within (file-picker)",
           "type": "array",
           "items": {
            "type": "string"
           }
          },
          "url": {
           "description": "Starting URL to navigate to (browser-use)",
           "type": "string"
          },
          "prompts": {
           "description": "Array of strategy prompts (editor-multi-prompt, code-reviewer-multi-prompt)",
           "type": "array",
           "items": {
            "type": "string"
           }
          }
         },
         "additionalProperties": false
        }
       },
       "required": [
        "agent_type"
       ],
       "additionalProperties": false
      }
     }
    },
    "required": [
     "agents"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "read_files",
   "description": "Example:\n<read_files_params_example>\n{\n  \"paths\": [\n    \"path/to/file1.ts\",\n    \"path/to/file2.ts\"\n  ]\n}\n</read_files_params_example>",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Read multiple files from disk. Returned file content shares a 20,000 estimated-token limit and a 100,000-character hard limit. Prefer the smallest relevant set of files and use code_search for targeted discovery.",
    "type": "object",
    "properties": {
     "paths": {
      "description": "List of file paths to read.",
      "type": "array",
      "items": {
       "description": "File path to read. Prefer paths relative to the **project root**; absolute paths inside the project are accepted, but paths outside the project will not work.",
       "type": "string",
       "minLength": 1
      }
     }
    },
    "required": [
     "paths"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "read_subtree",
   "description": "Example:\n<read_subtree_params_example>\n{\n  \"paths\": [\n    \"src\",\n    \"package.json\"\n  ],\n  \"maxTokens\": 4000\n}\n</read_subtree_params_example>\n\nPurpose: Read a directory subtree and return a blob containing subdirectories, file names, and parsed variable/functions names from source files. For files, return only the parsed variable names. If no paths are provided, returns the entire project tree. The output is truncated to fit within the provided token budget.\n\n- Use this tool on particular subdirectories when you need to know all the nested files and directories. E.g. for a refactoring task, or to understand a particular part of the codebase.\n- In normal use, don't set maxTokens beyond 10,000.",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Read one or more directory subtrees (as a blob including subdirectories, file names, and parsed variables within each source file) or return parsed variable names for files. If no paths are provided, returns the entire project tree.",
    "type": "object",
    "properties": {
     "paths": {
      "description": "List of paths to directories or files. Relative to the project root. If omitted, the entire project tree is used.",
      "type": "array",
      "items": {
       "type": "string"
      }
     },
     "maxTokens": {
      "description": "Maximum token budget for the subtree blob; the tree will be truncated to fit within this budget by first dropping file variables and then removing the most-nested files and directories.",
      "default": 4000,
      "type": "integer",
      "exclusiveMinimum": 0,
      "maximum": 9007199254740991
     }
    },
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "write_todos",
   "description": "Use this tool to track your objectives through an ordered step-by-step plan. Call this tool after you have gathered context on the user's request to plan out the implementation steps for the user's request.\n\nAfter completing each todo step, call this tool again to update the list and mark that task as completed. Note that each time you call this tool, rewrite ALL todos with their current status.\n\nUse this tool frequently as you work through tasks to update the list of todos with their current status. Doing this is extremely useful because it helps you stay on track and complete all the requirements of the user's request. It also helps inform the user of your plans and the current progress, which they want to know at all times.\n\nExample:\n<write_todos_params_example>\n{\n  \"todos\": [\n    {\n      \"task\": \"Create new implementation in foo.ts\",\n      \"completed\": true\n    },\n    {\n      \"task\": \"Update bar.ts to use the new implementation\",\n      \"completed\": false\n    },\n    {\n      \"task\": \"Write tests for the new implementation\",\n      \"completed\": false\n    },\n    {\n      \"task\": \"Run the tests to verify the new implementation\",\n      \"completed\": false\n    }\n  ]\n}\n</write_todos_params_example>",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Write a todo list to track tasks for multi-step implementations. Use this frequently to maintain an updated step-by-step plan.",
    "type": "object",
    "properties": {
     "todos": {
      "description": "List of todos with their completion status. Add ALL of the applicable tasks to the list, so you don't forget to do anything. Try to order the todos the same way you will complete them. Do not mark todos as completed if you have not completed them yet!",
      "type": "array",
      "items": {
       "type": "object",
       "properties": {
        "task": {
         "description": "Description of the task",
         "type": "string"
        },
        "completed": {
         "description": "Whether the task is completed",
         "type": "boolean"
        }
       },
       "required": [
        "task",
        "completed"
       ],
       "additionalProperties": false
      }
     }
    },
    "required": [
     "todos"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "suggest_followups",
   "description": "Suggest clickable followup prompts to the user. When the user clicks a suggestion, it sends that prompt as a new user message.\n\nUse this tool after completing a task to suggest what the user might want to do next. Good suggestions include:\n- Alternatives to the latest implementation like \"Cache the data to local storage instead\"\n- Related features like \"Add a hover card to show the data from the state\"\n- Cleanup opportunities like \"Refactor app.ts into multiple files\"\n- Testing suggestions like \"Add unit tests for this change\"\n- \"Continue with the next step\" - when there are more steps in a plan\n\nDon't include suggestions like:\n- \"Commit these changes\"\n- \"Test x\" without saying how you would test the changes (unit test, script, or something else?). Remember, this is a prompt for the assistant to do. Don't suggest manual testing that the user would have to do.\n\nTry to make different suggestions than you did in past steps. That's because users can still click previous suggestions if they want to.\n\nAim for around 3 suggestions. The suggestions persist and remain clickable, with clicked ones visually updated to show they were used.\n\n<suggest_followups_params_example>\n{\n  \"followups\": [\n    {\n      \"prompt\": \"Continue with the next step\",\n      \"label\": \"Continue\"\n    },\n    {\n      \"prompt\": \"Add unit tests for the new UserService class\",\n      \"label\": \"Add tests\"\n    },\n    {\n      \"prompt\": \"Refactor the authentication logic into a separate module\",\n      \"label\": \"Refactor auth\"\n    }\n  ]\n}\n</suggest_followups_params_example>",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Suggest clickable followup prompts to the user. Each followup becomes a card the user can click to send that prompt.",
    "type": "object",
    "properties": {
     "followups": {
      "description": "List of suggested followup prompts the user can click to send",
      "minItems": 1,
      "type": "array",
      "items": {
       "type": "object",
       "properties": {
        "prompt": {
         "description": "The full prompt text to send as a user message when clicked",
         "type": "string"
        },
        "label": {
         "description": "Short display label for the card (defaults to truncated prompt if not provided)",
         "type": "string"
        }
       },
       "required": [
        "prompt"
       ],
       "additionalProperties": false
      }
     }
    },
    "required": [
     "followups"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "str_replace",
   "description": "Use this tool to make edits within existing files.\n\nImportant:\nIf you are making multiple edits in a row to a file, use only one str_replace call with multiple replacements instead of multiple str_replace tool calls.\n\nExample:\n<str_replace_params_example>\n{\n  \"path\": \"path/to/file\",\n  \"replacements\": [\n    {\n      \"oldString\": \"This is the old string\",\n      \"newString\": \"This is the new string\"\n    },\n    {\n      \"oldString\": \"\\n\\t\\t// @codebuff delete this log line please\\n\\t\\tconsole.log(\\\"Hello, world!\\\");\\n\",\n      \"newString\": \"\\n\"\n    },\n    {\n      \"oldString\": \"\\nfoo:\",\n      \"newString\": \"\\nbar:\",\n      \"allowMultiple\": true\n    }\n  ]\n}\n</str_replace_params_example>",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Replace strings in a file with new strings.",
    "type": "object",
    "properties": {
     "path": {
      "description": "The path to the file to edit.",
      "type": "string",
      "minLength": 1
     },
     "replacements": {
      "description": "Array of replacements to make.",
      "minItems": 1,
      "type": "array",
      "items": {
       "description": "Pair of oldString and newString values.",
       "type": "object",
       "properties": {
        "oldString": {
         "description": "The string to replace. This must be an *exact match* of the string you want to replace, including whitespace and punctuation.",
         "type": "string",
         "minLength": 1
        },
        "newString": {
         "description": "The string to replace the corresponding oldString with. Can be empty to delete.",
         "type": "string"
        },
        "allowMultiple": {
         "description": "Whether to allow multiple replacements of oldString.",
         "default": false,
         "type": "boolean"
        }
       },
       "required": [
        "oldString",
        "newString"
       ],
       "additionalProperties": false
      }
     }
    },
    "required": [
     "path",
     "replacements"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "write_file",
   "description": "Create or replace a file with the given content.\n\nFormat the `content` parameter with the entire content of the file.\n\n#### Additional Info\n\nDo not use this tool to delete or rename a file. Instead run a terminal command for that.\n\nExamples:\n\nExample 1 - Simple file creation:\n<write_file_params_example>\n{\n  \"path\": \"new-file.ts\",\n  \"instructions\": \"Prints Hello, world\",\n  \"content\": \"console.log(\\\"Hello, world!\\\");\"\n}\n</write_file_params_example>\n\nExample 2 - Overwriting a file:\n<write_file_params_example>\n{\n  \"path\": \"foo.ts\",\n  \"instructions\": \"Update foo function\",\n  \"content\": \"function foo() {\\n  doSomethingNew();\\n}\\n  \\nfunction bar() {\\n  doSomethingOld();\\n}\\n\"\n}\n</write_file_params_example>",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Create or overwrite a file with the given content.",
    "type": "object",
    "properties": {
     "path": {
      "description": "Path to the file relative to the **project root**",
      "type": "string",
      "minLength": 1
     },
     "instructions": {
      "description": "What the change is intended to do in only one sentence.",
      "type": "string"
     },
     "content": {
      "description": "Complete file content to write to the file.",
      "type": "string"
     }
    },
    "required": [
     "path",
     "instructions",
     "content"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "ask_user",
   "description": "Ask the user multiple choice questions and pause execution until they respond. Supports both single-select (radio) and multi-select (checkbox) modes.\n\nThe user can either:\n- Select one option (single-select mode, default)\n- Select multiple options (multi-select mode, set multiSelect: true)\n- Type a custom answer in the \"Other\" text field\n- Skip the questions to provide different instructions instead\n\nIMPORTANT: Do NOT include options like \"Custom\", \"Other\", \"None of the above\", or similar catch-all options. The UI automatically provides a \"Custom\" text input field for users to type their own answer. Including such options would be redundant and confusing.\n\nSingle-select example:\n<ask_user_params_example>\n{\n  \"questions\": [\n    {\n      \"question\": \"Which authentication method should we use?\",\n      \"header\": \"Auth method\",\n      \"options\": [\n        {\n          \"label\": \"JWT tokens\",\n          \"description\": \"Stateless tokens stored in localStorage\"\n        },\n        {\n          \"label\": \"Session cookies\",\n          \"description\": \"Server-side sessions with httpOnly cookies\"\n        },\n        {\n          \"label\": \"OAuth2\",\n          \"description\": \"Third-party authentication (Google, GitHub, etc.)\"\n        }\n      ]\n    }\n  ]\n}\n</ask_user_params_example>\n\nMulti-select example:\n<ask_user_params_example>\n{\n  \"questions\": [\n    {\n      \"question\": \"Which features should we implement?\",\n      \"header\": \"Features\",\n      \"options\": [\n        {\n          \"label\": \"Rate limiting\"\n        },\n        {\n          \"label\": \"Caching\"\n        },\n        {\n          \"label\": \"Logging\"\n        },\n        {\n          \"label\": \"Monitoring\"\n        }\n      ],\n      \"multiSelect\": true\n    }\n  ]\n}\n</ask_user_params_example>",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Ask the user a list of multiple choice questions. Each question must have at least 2 options. The agent execution will pause until the user submits their answers.",
    "type": "object",
    "properties": {
     "questions": {
      "description": "List of multiple choice questions to ask the user",
      "minItems": 1,
      "type": "array",
      "items": {
       "type": "object",
       "properties": {
        "question": {
         "description": "The question to ask the user",
         "type": "string"
        },
        "header": {
         "description": "Short label (max 12 chars) displayed as a chip/tag. Example: \"Auth method\"",
         "type": "string",
         "maxLength": 18
        },
        "options": {
         "description": "Array of answer options with label and optional description.",
         "type": "array",
         "items": {
          "type": "object",
          "properties": {
           "label": {
            "description": "The display text for this option",
            "type": "string"
           },
           "description": {
            "description": "Explanation shown when option is focused",
            "type": "string"
           }
          },
          "required": [
           "label"
          ],
          "additionalProperties": false
         }
        },
        "multiSelect": {
         "description": "If true, allows selecting multiple options (checkbox). If false, single selection only (radio).",
         "default": false,
         "type": "boolean"
        },
        "validation": {
         "description": "Validation rules for \"Other\" text input",
         "type": "object",
         "properties": {
          "maxLength": {
           "description": "Maximum length for \"Other\" text input",
           "type": "number"
          },
          "minLength": {
           "description": "Minimum length for \"Other\" text input",
           "type": "number"
          },
          "pattern": {
           "description": "Regex pattern for \"Other\" text input",
           "type": "string"
          },
          "patternError": {
           "description": "Custom error message when pattern fails",
           "type": "string"
          }
         },
         "additionalProperties": false
        }
       },
       "required": [
        "question",
        "options"
       ],
       "additionalProperties": false
      }
     }
    },
    "required": [
     "questions"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "read_url",
   "description": "Purpose: Fetch a URL returned by web_search and extract the readable page text so you can answer with source-backed evidence.\n\nUse this after web_search when snippets are not enough. Prefer authoritative, relevant pages from the search results. The tool follows redirects, extracts titles and metadata, strips scripts/styles/navigation boilerplate from HTML, and returns normalized readable text.\n\nDo not use run_terminal_command with curl just to inspect web pages; use read_url instead. If read_url reports unsupported content or extraction failure, then choose a different search result or explain the limitation.\n\nExample:\n<read_url_params_example>\n{\n  \"url\": \"https://react.dev/reference/react/useActionState\",\n  \"max_chars\": 12000\n}\n</read_url_params_example>",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Fetch a URL and extract readable text from the page.",
    "type": "object",
    "properties": {
     "url": {
      "description": "The full http:// or https:// URL to fetch and extract readable text from.",
      "type": "string",
      "format": "uri"
     },
     "max_chars": {
      "description": "Maximum number of extracted text characters to return. Defaults to 20000.",
      "default": 20000,
      "type": "integer",
      "minimum": 1000,
      "maximum": 50000
     }
    },
    "required": [
     "url"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "skill",
   "description": "Load a skill by name to get its full instructions. Skills provide reusable behaviors and domain-specific knowledge that you can use to complete tasks.\n\nThe following are the pre-loaded skills available at session start:\nThere are no skills available. Do not use this tool because there are no skills to load.\n\nNote: You can load any skill by name, including ones created or installed during this session (e.g. via `npx skills add`). The skill is always read fresh from disk, so its latest contents are used.\n\nExample:\n<skill_params_example>\n{\n  \"name\": \"git-release\"\n}\n</skill_params_example>",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Load a skill by name to get its full instructions. Skills provide reusable behaviors and instructions.",
    "type": "object",
    "properties": {
     "name": {
      "description": "The name of the skill to load",
      "type": "string",
      "minLength": 1
     }
    },
    "required": [
     "name"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "set_output",
   "description": "Subagents must use this tool as it is the only way to report any findings. Nothing else you write will be visible to the user/parent agent.\n\nNote that the output schema is provided dynamically in a user prompt further down in the conversation. Be sure to follow what the latest output schema is when using this tool.\n\nPlease set the output with all the information and analysis you want to pass on. If you just want to send a simple message, use an object with the key \"message\" and value of the message you want to send.\nExample:\n<set_output_params_example>\n{\n  \"message\": \"I found a bug in the code!\"\n}\n</set_output_params_example>",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "JSON object to set as the agent output. The shape of the parameters are specified dynamically further down in the conversation. This completely replaces any previous output. If the agent was spawned, this value will be passed back to its parent. If the agent has an outputSchema defined, the output will be validated against it.",
    "type": "object",
    "properties": {
     "data": {
      "type": "object",
      "propertyNames": {
       "type": "string"
      },
      "additionalProperties": false
     }
    },
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "list_directory",
   "description": "Lists all files and directories in the specified path. Useful for exploring directory structure and finding files.\n\nExample:\n<list_directory_params_example>\n{\n  \"path\": \"src/components\"\n}\n</list_directory_params_example>\n\n<list_directory_params_example>\n{\n  \"path\": \".\"\n}\n</list_directory_params_example>",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "List files and directories in the specified path. Returns separate arrays of file names and directory names.",
    "type": "object",
    "properties": {
     "path": {
      "description": "Directory path to list, relative to the project root.",
      "type": "string"
     }
    },
    "required": [
     "path"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "glob",
   "description": "Example:\n<glob_params_example>\n{\n  \"pattern\": \"**/*.test.ts\"\n}\n</glob_params_example>\n\nPurpose: Search for files matching a glob pattern to discover files by name patterns rather than content.\nUse cases:\n- Find all files with a specific extension (e.g., \"*.js\", \"*.test.ts\")\n- Locate files in specific directories (e.g., \"src/**/*.ts\")\n- Find files with specific naming patterns (e.g., \"**/test_*.go\", \"**/*-config.json\")\n- Discover test files, configuration files, or other files with predictable naming\n\nGlob patterns support:\n- * matches any characters except /\n- ** matches any characters including /\n- ? matches a single character\n- [abc] matches one of the characters in brackets\n- {a,b} matches one of the comma-separated patterns\n\nThis tool is fast and works well for discovering files by name patterns.",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Search for files matching a glob pattern. Returns matching file paths sorted by modification time.",
    "type": "object",
    "properties": {
     "pattern": {
      "description": "Glob pattern to match files against (e.g., *.js, src/glob/*.ts, glob/test/glob/*.go).",
      "type": "string",
      "minLength": 1
     },
     "cwd": {
      "description": "Optional working directory to search within, relative to project root. If not provided, searches from project root.",
      "type": "string"
     }
    },
    "required": [
     "pattern"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "render_ui",
   "description": "Render a small interactive UI widget for the user.\n\nCurrently supported widgets:\n- button: renders a clickable button with text and an http(s) link.\n\nUse this when the user should click a clear action, such as opening a generated report, documentation page, checkout page, deployment URL, preview, or dashboard.\n\nFor a service selected from a gravity_index search, do not copy its opaque URL. Set `link` to a gravity_index reference containing the exact `search_id` and selected `service_slug`. The runtime verifies the selection against prior tool results and substitutes the exact stored click URL. Call render_ui only after deciding which service to recommend.\n\nColor variants:\n- primary: the main action\n- secondary: a lower-emphasis action\n\nKeep button text short and action-oriented.\n\n<render_ui_params_example>\n{\n  \"widget\": {\n    \"type\": \"button\",\n    \"text\": \"Open preview\",\n    \"link\": \"https://example.com/preview\",\n    \"variant\": \"primary\"\n  }\n}\n</render_ui_params_example>\n\n<render_ui_params_example>\n{\n  \"widget\": {\n    \"type\": \"button\",\n    \"text\": \"Get your Resend API key\",\n    \"link\": {\n      \"source\": \"gravity_index\",\n      \"search_id\": \"search_id_from_gravity_index\",\n      \"service_slug\": \"resend\"\n    },\n    \"variant\": \"primary\"\n  }\n}\n</render_ui_params_example>",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Render a small interactive UI widget for the user.",
    "type": "object",
    "properties": {
     "widget": {
      "description": "The UI widget to render.",
      "oneOf": [
       {
        "type": "object",
        "properties": {
         "type": {
          "description": "Widget type. Currently, the only supported widget is button.",
          "type": "string",
          "const": "button"
         },
         "text": {
          "description": "Short button label shown to the user.",
          "type": "string",
          "minLength": 1,
          "maxLength": 80
         },
         "variant": {
          "description": "Theme-aware color treatment. Use primary for the main action and secondary for lower-emphasis actions.",
          "default": "primary",
          "type": "string",
          "enum": [
           "primary",
           "secondary"
          ]
         },
         "link": {
          "description": "Either an http(s) URL or a gravity_index reference that the runtime resolves to the exact tracked click URL.",
          "anyOf": [
           {
            "type": "string",
            "format": "uri"
           },
           {
            "description": "A reference to a tracked click URL in a prior gravity_index search result. The runtime substitutes the exact stored URL.",
            "type": "object",
            "properties": {
             "source": {
              "type": "string",
              "const": "gravity_index"
             },
             "search_id": {
              "description": "The exact search_id returned by gravity_index.",
              "type": "string",
              "minLength": 1
             },
             "service_slug": {
              "description": "The exact slug of the recommendation or option selected from that search.",
              "type": "string",
              "minLength": 1
             }
            },
            "required": [
             "source",
             "search_id",
             "service_slug"
            ]
           }
          ]
         }
        },
        "required": [
         "type",
         "text",
         "link"
        ]
       }
      ]
     }
    },
    "required": [
     "widget"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "gravity_index",
   "description": "Purpose: Use the Gravity Index to discover, inspect, and report integrations for third-party developer services such as databases, auth, payments, hosting, email, cache, monitoring, analytics, AI, storage, CMS, search, realtime, background jobs, infrastructure, CRM, support, productivity, commerce, video, webhooks, and SMS.\n\nChoose the action:\n- `search`: Use when the user asks for a recommendation or when you need to choose a provider before integrating it. Returns a reasoned recommendation with install guidance, env vars, and a setup/conversion URL. Include stack and constraints in `query`. Pass `search_id` from a previous search for follow-up questions.\n- `browse`: Use to list catalog services by `category` and/or keyword `q`. Good when the user wants options or a category-scoped picker.\n- `list_categories`: Use to see available categories and service counts.\n- `get_service`: Use when you already know a service slug and need full detail, env vars, website, docs URL, and install metadata.\n- `report_integration`: Use after you have actually completed and verified an integration from a previous search. Pass the original `search_id` and the service slug as `integrated_slug`.\n\nImportant setup-link behavior:\n- Search results may include `credential_request.setup_url`, `recommendation.click_url`, and/or top-level `click_url`, the tracked setup link the user should visit to create an account and get API credentials.\n- A search can contain a recommendation and several options, and you may search more than once. Decide which single service you are actually recommending before rendering a CTA.\n- When `render_ui` is available, call it once for the selected service. Use a gravity_index link reference with the exact `search_id` and selected `service_slug`; never transcribe the opaque URL. The runtime verifies the selection and substitutes the exact stored click URL.\n- When `render_ui` is unavailable, preserve and present the exact returned tracked URL in a normal markdown link.\n- Do not replace the tracked setup link with the vendor homepage and do not auto-follow it.\n- Ask the user to paste the required env vars from `credential_request.required_env_vars` back so you can finish setup.\n\nImplementation guidance:\n- Gravity returns reasoning, `install.steps`, `install.env_vars`, and `credential_request`. Execute the install steps you can perform locally, then pause only for missing user credentials.\n- Use the returned `docs_url`, existing codebase conventions, and package/docs research to fill any gaps in the install instructions.\n- For browsing results, use `get_service` on promising slugs before making a final recommendation if details matter.\n\nExamples:\n<gravity_index_params_example>\n{\n  \"action\": \"search\",\n  \"query\": \"transactional email API with a generous free tier for a Next.js app\"\n}\n</gravity_index_params_example>\n\n<gravity_index_params_example>\n{\n  \"action\": \"browse\",\n  \"category\": \"Email\",\n  \"q\": \"send\"\n}\n</gravity_index_params_example>\n\n<gravity_index_params_example>\n{\n  \"action\": \"get_service\",\n  \"slug\": \"sendgrid\"\n}\n</gravity_index_params_example>\n\n<gravity_index_params_example>\n{\n  \"action\": \"report_integration\",\n  \"search_id\": \"search_id_from_previous_search\",\n  \"integrated_slug\": \"sendgrid\"\n}\n</gravity_index_params_example>",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Use the Gravity Index tool discovery and install API.",
    "type": "object",
    "properties": {
     "action": {
      "description": "Which Gravity Index operation to perform. search: recommend a provider; browse: list catalog services; list_categories: list categories with counts; get_service: full detail for a known slug; report_integration: report a completed integration.",
      "type": "string",
      "enum": [
       "search",
       "browse",
       "list_categories",
       "get_service",
       "report_integration"
      ]
     },
     "query": {
      "description": "For action \"search\": what the user needs, including stack, constraints, and required capabilities when known. Example: \"serverless database with branching for a Next.js app\".",
      "type": "string",
      "minLength": 1,
      "maxLength": 1000
     },
     "search_id": {
      "description": "For action \"search\": continue a previous Gravity Index search as a follow-up. For action \"report_integration\": the search_id from the earlier search result (required).",
      "type": "string"
     },
     "context": {
      "description": "For action \"search\": optional structured JSON context about the project, stack, or constraints.",
      "allOf": [
       {
        "type": "object",
        "propertyNames": {
         "type": "string"
        },
        "additionalProperties": {
         "anyOf": [
          {
           "type": "null"
          },
          {
           "type": "string"
          },
          {
           "type": "number"
          },
          {
           "type": "boolean"
          },
          {},
          {
           "type": "array",
           "items": {}
          }
         ]
        }
       }
      ]
     },
     "category": {
      "description": "For action \"browse\": optional category filter, e.g. Database, Auth, Payments, Hosting, Email, Cache, Monitoring, Analytics, AI, Storage, CMS, Search, Realtime, Background Jobs, Infrastructure, CRM, Support, Productivity, Commerce, Video, Webhooks, SMS.",
      "type": "string"
     },
     "q": {
      "description": "For action \"browse\": optional keyword filter, e.g. sendgrid or postgres.",
      "type": "string"
     },
     "slug": {
      "description": "For action \"get_service\": service slug, e.g. supabase, stripe, sendgrid (required).",
      "type": "string",
      "minLength": 1
     },
     "integrated_slug": {
      "description": "For action \"report_integration\": slug of the service that was actually integrated (required).",
      "type": "string",
      "minLength": 1
     }
    },
    "required": [
     "action"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "file_picker",
   "description": "Spawn to find relevant files in a codebase related to the prompt. Outputs up to 12 file paths with short summaries for each file. Cannot do string searches on the codebase, but does a fuzzy search. Unless you know which directories are relevant, omit the directories parameter. This agent is extremely effective at finding files in the codebase that could be relevant to the prompt.",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Spawn to find relevant files in a codebase related to the prompt. Outputs up to 12 file paths with short summaries for each file. Cannot do string searches on the codebase, but does a fuzzy search. Unless you know which directories are relevant, omit the directories parameter. This agent is extremely effective at finding files in the codebase that could be relevant to the prompt.",
    "type": "object",
    "properties": {
     "prompt": {
      "description": "A description of the files you need to find. Be more broad for better results: instead of \"Find x file\" say \"Find x file and related files\". This agent is designed to help you find several files that could be relevant to the prompt.",
      "type": "string"
     },
     "params": {
      "type": "object",
      "properties": {
       "directories": {
        "description": "Optional list of paths to directories to look within. If omitted, the entire project tree is used.",
        "type": "array",
        "items": {
         "type": "string"
        }
       }
      },
      "additionalProperties": false
     }
    },
    "required": [
     "prompt",
     "params"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "code_searcher",
   "description": "Mechanically runs multiple code search queries (using ripgrep line-oriented search) and returns up to 250 results across all source files, showing each line that matches the search pattern. Excludes git-ignored files. You MUST pass searchQueries in params. Example input: { \"params\": { \"searchQueries\": [{ \"pattern\": \"createUser\", \"flags\": \"-g *.ts\" }, { \"pattern\": \"deleteUser\", \"flags\": \"-g *.ts\" }, { \"pattern\": \"UserSchema\", \"maxResults\": 5 }] } }",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Mechanically runs multiple code search queries (using ripgrep line-oriented search) and returns up to 250 results across all source files, showing each line that matches the search pattern. Excludes git-ignored files. You MUST pass searchQueries in params. Example input: { \"params\": { \"searchQueries\": [{ \"pattern\": \"createUser\", \"flags\": \"-g *.ts\" }, { \"pattern\": \"deleteUser\", \"flags\": \"-g *.ts\" }, { \"pattern\": \"UserSchema\", \"maxResults\": 5 }] } }",
    "type": "object",
    "properties": {
     "params": {
      "type": "object",
      "properties": {
       "searchQueries": {
        "description": "Array of code search queries to execute",
        "type": "array",
        "items": {
         "type": "object",
         "properties": {
          "pattern": {
           "description": "The pattern to search for",
           "type": "string"
          },
          "flags": {
           "description": "Optional ripgrep flags to customize the search (e.g., \"-i\" for case-insensitive, \"-g *.ts -g *.js\" for TypeScript and JavaScript files only, \"-g !*.test.ts\" to exclude Typescript test files,  \"-A 3\" for 3 lines after match, \"-B 2\" for 2 lines before match).",
           "type": "string"
          },
          "cwd": {
           "description": "Optional working directory to search within, relative to the project root. Defaults to searching the entire project",
           "type": "string"
          },
          "maxResults": {
           "description": "Maximum number of results to return per file. Defaults to 15. There is also a global limit of 250 results across all files",
           "type": "number"
          }
         },
         "required": [
          "pattern"
         ],
         "additionalProperties": false
        }
       }
      },
      "required": [
       "searchQueries"
      ],
      "additionalProperties": false
     }
    },
    "required": [
     "params"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "researcher_web",
   "description": "Browses the web to find relevant information.",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Browses the web to find relevant information.",
    "type": "object",
    "properties": {
     "prompt": {
      "description": "A question you would like answered using web search",
      "type": "string"
     }
    },
    "required": [
     "prompt"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "researcher_docs",
   "description": "Expert at reading technical documentation of major public libraries and frameworks to find relevant information. (e.g. React, MongoDB, Postgres, etc.)",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Expert at reading technical documentation of major public libraries and frameworks to find relevant information. (e.g. React, MongoDB, Postgres, etc.)",
    "type": "object",
    "properties": {
     "prompt": {
      "description": "A question you would like answered using technical documentation.",
      "type": "string"
     }
    },
    "required": [
     "prompt"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "basher",
   "description": "Runs a single terminal command and returns its output. A lightweight shell command executor. Every basher spawn MUST include params: { command: \"<shell>\" }. Add what_to_summarize only when you expect long or noisy output (full test suites, builds, large logs) and want an LLM to pull out the relevant part; for ordinary commands leave it off and read the output yourself. Short output is returned raw either way.",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Runs a single terminal command and returns its output. A lightweight shell command executor. Every basher spawn MUST include params: { command: \"<shell>\" }. Add what_to_summarize only when you expect long or noisy output (full test suites, builds, large logs) and want an LLM to pull out the relevant part; for ordinary commands leave it off and read the output yourself. Short output is returned raw either way.",
    "type": "object",
    "properties": {
     "params": {
      "type": "object",
      "properties": {
       "command": {
        "description": "The terminal command to run in bash shell. Don't forget this field!",
        "type": "string"
       },
       "what_to_summarize": {
        "description": "Optional. What information from the command output is desired -- be specific about what to look for or extract. Only worth setting when you expect long or noisy output (a full test suite, a build, a large log); omit it for ordinary commands and read the output yourself. Output that is already short is returned raw either way, so setting this never hides anything from you.",
        "type": "string"
       },
       "timeout_seconds": {
        "description": "Set to -1 for no timeout. Default 30",
        "type": "number"
       }
      },
      "required": [
       "command"
      ],
      "additionalProperties": false
     }
    },
    "required": [
     "params"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "tmux_cli",
   "description": "General-purpose agent that uses tmux to interact with and test CLI applications.\n\n**Your responsibilities as the parent agent:**\n1. If `scriptIssues` is not empty, check the error details and re-run the agent\n2. Use `read_files` on the capture paths to see what the CLI displayed\n3. Re-run the agent after fixing any issues\n4. Check the `lessons` array for advice on how to improve future runs\n\n**Note:** Capture files are saved to `/tmp/`. Use `run_terminal_command` with `cat` to read them if `read_files` doesn't support absolute paths.\n\n**When spawning this agent**, provide as much advice as possible in the prompt about how to test the CLI, including lessons from any previous runs of tmux-cli (e.g., timing adjustments, commands that didn't work, expected output patterns). This helps the agent avoid repeating mistakes.\n\n**Orphaned session cleanup:** If the agent fails or times out, the tmux session may linger. Run `tmux kill-session -t <sessionName>` to clean up. The session name is in the agent's output.",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "General-purpose agent that uses tmux to interact with and test CLI applications.\n\n**Your responsibilities as the parent agent:**\n1. If `scriptIssues` is not empty, check the error details and re-run the agent\n2. Use `read_files` on the capture paths to see what the CLI displayed\n3. Re-run the agent after fixing any issues\n4. Check the `lessons` array for advice on how to improve future runs\n\n**Note:** Capture files are saved to `/tmp/`. Use `run_terminal_command` with `cat` to read them if `read_files` doesn't support absolute paths.\n\n**When spawning this agent**, provide as much advice as possible in the prompt about how to test the CLI, including lessons from any previous runs of tmux-cli (e.g., timing adjustments, commands that didn't work, expected output patterns). This helps the agent avoid repeating mistakes.\n\n**Orphaned session cleanup:** If the agent fails or times out, the tmux session may linger. Run `tmux kill-session -t <sessionName>` to clean up. The session name is in the agent's output.",
    "type": "object",
    "properties": {
     "prompt": {
      "description": "What to do with the CLI application (e.g., \"run /help and verify output\", \"send a prompt and capture the response\")",
      "type": "string"
     },
     "params": {
      "type": "object",
      "properties": {
       "command": {
        "description": "The CLI command to start in the tmux session (e.g., \"python app.py\", \"node server.js\", \"my-cli --interactive\")",
        "type": "string"
       }
      },
      "additionalProperties": false
     }
    },
    "required": [
     "prompt",
     "params"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "browser_use",
   "description": "Browser automation agent that uses Chrome DevTools to interact with web pages.\n\n**Use cases:**\n- Verify that code changes render correctly in the browser\n- Test web application functionality (click buttons, fill forms, check results)\n- Navigate websites and extract information\n- Check for console errors, broken layouts, or missing elements\n- Validate responsive design and accessibility\n\n**Your responsibilities as the parent agent:**\n1. Provide a clear task description and optionally a starting URL\n2. Check the `results` array for step-by-step outcomes\n3. Check `consoleErrors` for any JavaScript errors found\n4. Check `lessons` for advice on improving future runs\n\n**Requirements:** Chrome must be installed. Check System Info for \"Chrome: installed\" before spawning. If Chrome is not found, do NOT spawn this agent \u2014 instead inform the user that the browser-use agent requires Google Chrome or Chromium to be installed.",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Browser automation agent that uses Chrome DevTools to interact with web pages.\n\n**Use cases:**\n- Verify that code changes render correctly in the browser\n- Test web application functionality (click buttons, fill forms, check results)\n- Navigate websites and extract information\n- Check for console errors, broken layouts, or missing elements\n- Validate responsive design and accessibility\n\n**Your responsibilities as the parent agent:**\n1. Provide a clear task description and optionally a starting URL\n2. Check the `results` array for step-by-step outcomes\n3. Check `consoleErrors` for any JavaScript errors found\n4. Check `lessons` for advice on improving future runs\n\n**Requirements:** Chrome must be installed. Check System Info for \"Chrome: installed\" before spawning. If Chrome is not found, do NOT spawn this agent \u2014 instead inform the user that the browser-use agent requires Google Chrome or Chromium to be installed.",
    "type": "object",
    "properties": {
     "prompt": {
      "description": "What to do in the browser (e.g., \"Navigate to localhost:3000 and verify the login form works\")",
      "type": "string"
     },
     "params": {
      "type": "object",
      "properties": {
       "url": {
        "description": "Starting URL to navigate to (e.g., \"http://localhost:3000\"). If not provided, the agent will determine the URL from the prompt.",
        "type": "string"
       }
      },
      "additionalProperties": false
     }
    },
    "required": [
     "prompt",
     "params"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "code_reviewer_deepseek_flash",
   "description": "Reviews file changes and responds with critical feedback. Use this after making any significant change to the codebase; otherwise, no need to use this agent for minor changes since it takes a second.",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Reviews file changes and responds with critical feedback. Use this after making any significant change to the codebase; otherwise, no need to use this agent for minor changes since it takes a second.",
    "type": "object",
    "properties": {
     "prompt": {
      "description": "What should be reviewed. Be brief.",
      "type": "string"
     }
    },
    "required": [
     "prompt"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "thinker_gpt",
   "description": "Does deep thinking given the current conversation history and a specific prompt to focus on. Use this to help you solve a specific problem. You must gather any relevant context before spawning this agent because the thinker agent has no access to tools. You can keep the prompt very short, because the thinker agent can see the entire conversation history for context.",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Does deep thinking given the current conversation history and a specific prompt to focus on. Use this to help you solve a specific problem. You must gather any relevant context before spawning this agent because the thinker agent has no access to tools. You can keep the prompt very short, because the thinker agent can see the entire conversation history for context.",
    "type": "object",
    "properties": {
     "prompt": {
      "description": "The problem you are trying to solve, very briefly. No need to provide context, as the thinker agent can see the entire conversation history.",
      "type": "string"
     }
    },
    "required": [
     "prompt"
    ],
    "additionalProperties": false
   }
  }
 },
 {
  "type": "function",
  "function": {
   "name": "context_pruner",
   "description": "Spawn this agent between steps to prune context, summarizing the conversation into a condensed format when context exceeds the limit.",
   "parameters": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Spawn this agent between steps to prune context, summarizing the conversation into a condensed format when context exceeds the limit.",
    "type": "object",
    "properties": {
     "params": {
      "type": "object",
      "properties": {
       "maxContextLength": {
        "type": "number"
       },
       "assistantToolBudget": {
        "type": "number"
       },
       "userBudget": {
        "type": "number"
       },
       "cacheExpiryMs": {
        "type": "number"
       }
      },
      "additionalProperties": false
     }
    },
    "required": [
     "params"
    ],
    "additionalProperties": false
   }
  }
 }
]